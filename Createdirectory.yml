---
- hosts: ec2-100-27-11-122.compute-1.amazonaws.com
  become: true
  become_method: sudo
  tasks:
   - name : pwd
     shell: "pwd"

 #  - name : file module
    # shell: "mkdir -p /opt/ansible/b"
